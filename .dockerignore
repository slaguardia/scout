# Keep the build context lean. The image needs scout/ (the package) and web/dist/
# (the committed PWA build), so neither is excluded here.
.git
.claude
.venv
*.db
*.db-shm
*.db-wal
*.csv
backups/
web/node_modules/
*.log
__pycache__/
*.egg-info/
.pytest_cache/
.mypy_cache/
