# Binaries
/scout
/bin/
*.exe
*.test
*.out

# Go build cache
/vendor/

# Local DB + dumps
*.db
*.db-journal
*.sqlite
*.sqlite3
/data/
/dumps/
# source CSV dumps (Crunchbase exports) dropped at the repo root
/*.csv

# Env / secrets
.env
.env.local
.env.*.local

# Personal, local-only config (real localized outreach template, scratch files).
# The tracked outreach-template.md stays a sanitized example; the real one with
# your credentials lives in local/ and is loaded via --outreach-template.
/local/

# SQLite WAL sidecars
*.db-wal
*.db-shm

# Claude Code worktrees (harness-managed checkouts)
.claude/worktrees/

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# PWA build (web/ → internal/web/dist embedded by go:embed)
/web/node_modules/
/web/public/manifest.webmanifest
/web/public/sw.js
