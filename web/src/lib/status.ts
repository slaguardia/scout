// "archived" is a protected built-in application_status ("stopped pursuing") — a
// normal terminal stage the user sets and filters like any other. Its one special
// behavior is server-side: a posting in it stops nagging for follow-ups.
export const ARCHIVED_STAGE = "archived";
